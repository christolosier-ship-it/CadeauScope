import { uid } from '../utils/ids.js';
import { nowIso } from '../utils/dates.js';
import { cleanOptionalNumber } from '../utils/numbers.js';
export function createIdea(data = {}) { const now = nowIso(); return { id: data.id || uid('idea'), personId: data.personId || '', title: (data.title || '').trim(), description: data.description || '', note: data.note || '', estimatedPrice: cleanOptionalNumber(data.estimatedPrice,{min:0}), priceText: data.priceText || '', link: String(data.link || '').trim(), photoId: data.photoId || '', categoryId: data.categoryId || 'cat_autre', occasionMode: data.occasionMode || 'none', occasionId: data.occasionId || '', occasionYear: data.occasionYear || '', interestLevel: Number.isFinite(Number(data.interestLevel || 2)) ? Number(data.interestLevel || 2) : 2, status: data.status || 'idee', createdAt: data.createdAt || now, updatedAt: now, boughtAt: data.boughtAt || '', wrappedAt: data.wrappedAt || '', offeredAt: data.offeredAt || '', archived: Boolean(data.archived) }; }
