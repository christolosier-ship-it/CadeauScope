import { uid } from '../utils/ids.js';
import { nowIso } from '../utils/dates.js';
export function createIdea(data = {}) { const now = nowIso(); const price = data.estimatedPrice === '' || data.estimatedPrice == null ? null : Number(data.estimatedPrice); return { id: data.id || uid('idea'), personId: data.personId || '', title: (data.title || '').trim(), description: data.description || '', note: data.note || '', estimatedPrice: Number.isFinite(price) ? price : null, priceText: data.priceText || '', link: data.link || '', photoId: data.photoId || '', categoryId: data.categoryId || 'cat_autre', occasionMode: data.occasionMode || 'none', occasionId: data.occasionId || '', occasionYear: data.occasionYear || '', interestLevel: Number(data.interestLevel || 2), status: data.status || 'idee', createdAt: data.createdAt || now, updatedAt: now, boughtAt: data.boughtAt || '', wrappedAt: data.wrappedAt || '', offeredAt: data.offeredAt || '', archived: Boolean(data.archived) }; }
