import { APP } from '../config.js';
import { nowIso } from '../utils/dates.js';
import { cleanOptionalNumber } from '../utils/numbers.js';
export function defaultSettings(data = {}) { const now = nowIso(); return { id: 'main', theme: data.theme || 'auto', currency: data.currency || 'EUR', customCurrencySymbol: data.customCurrencySymbol || '', eventWarningDays: cleanOptionalNumber(data.eventWarningDays ?? 30,{positive:true}) ?? 30, christmasWarningDays: cleanOptionalNumber(data.christmasWarningDays ?? 60,{positive:true}) ?? 60, defaultInterestLevel: Number.isFinite(Number(data.defaultInterestLevel ?? 2)) ? Number(data.defaultInterestLevel ?? 2) : 2, defaultIdeaStatus: data.defaultIdeaStatus || 'idee', defaultCategoryId: data.defaultCategoryId || 'cat_autre', exportPhotos: Boolean(data.exportPhotos), firstLaunchDone: Boolean(data.firstLaunchDone), legacyChristmasMigrated: Boolean(data.legacyChristmasMigrated), appVersion: APP.version, createdAt: data.createdAt || now, updatedAt: now }; }
