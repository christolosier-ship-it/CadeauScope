import { uid } from '../utils/ids.js';
import { nowIso } from '../utils/dates.js';
export function createPerson(data = {}) { const now = nowIso(); return { id: data.id || uid('person'), name: (data.name || '').trim(), relation: data.relation || 'autre', photoId: data.photoId || '', birthday: data.birthday || '', birthdayHasYear: Boolean(data.birthdayHasYear), christmasEnabled: data.christmasEnabled ?? true, clothingSize: data.clothingSize || '', shoeSize: data.shoeSize || '', interests: data.interests || '', favoriteColors: data.favoriteColors || '', favoriteBrands: data.favoriteBrands || '', avoid: data.avoid || '', notes: data.notes || '', archived: Boolean(data.archived), createdAt: data.createdAt || now, updatedAt: now }; }
